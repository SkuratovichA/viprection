{
  "name": "visual-preview-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plug-and-play GitHub Action: per-branch visual gallery + PR visual-diff comments.",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0"
  }
}
